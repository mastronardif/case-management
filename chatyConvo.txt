
7/7/26

----------------
give me the create table for patient.
from my similar table session where i have an id, jsondocId, sourcedocId do you rember that.

this projection
{
"projectionType": "Patient837P",
"targetDocument": "837P_LoopsSegments_X12",
"version": "1.0.0",
"fields": [
{
"target": "loops.2010BA.NM1.NM103",
"source": "subscriber.lastName"
},
{
"target": "loops.2010BA.NM1.NM104",
"source": "subscriber.firstName"
},
{
"target": "loops.2010BA.NM1.NM109",
"source": "subscriber.memberId"
},
{
"target": "loops.2010BA.DMG.DMG02",
"source": "subscriber.dob"
},
{
"target": "loops.2010BA.DMG.DMG03",
"source": "subscriber.gender"
},
{
"target": "loops.2010BA.N3.N301",
"source": "subscriber.address1"
},
{
"target": "loops.2010BA.N4.N401",
"source": "subscriber.city"
},
{
"target": "loops.2010BA.N4.N402",
"source": "subscriber.state"
},
{
"target": "loops.2010BA.N4.N403",
"source": "subscriber.zip"
}
]
}

-------------------

dotnet run -- --expression '777 (P) 779' --case-id 9

-------------------------
I will do something like this.  for the special cases
{ "target": "boxes.box24",  "source": "loops.2400",                                        "type": "array"  },
{ "target": "boxes.box2",   "source": "loops.2010BA.NM1.NM103,loops.2010BA.NM1.NM104",     "type": "concat", "separator": ", " },
{ "target": "boxes.box31",  "source": "loops.2310B.NM1.NM103,loops.2310B.NM1.NM104",       "type": "concat", "separator": ", " }

---------------
7/6/26
can you convert this to  have boxNN's.

---------------

7/5/26
give me a provider.json source doc
from this projection C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\42A\Provider837P.projection.json
where i can later project 
---------------
7/3/26



Session837P.projection.json
Authorization837P.projection.json
Provider837P.projection.json
Subscriber837P.projection.json
Claim837P.projection.json

give m the rule projection for session to "837P_LoopsSegments_X12"
I plan getting the left values from session json i have.
i plan on getting the authentication, provider info for the associated json of that data.
then i can add other session for said patient.

i can run each projection independently it will merge or add the data to "837P_LoopsSegments_X12"

what do you think? Do you know what i mean and do you need anymore information.

-----------------


-----------------------
--expression '(Z)[$jsonDocId, $ruleDocId, 525]' --json-doc-id 701 --rule-doc-id 525
> dotnet run --expression '(Z)[$jsonDocId, $ruleDocId, 525]' --json-doc-id 701 --rule-doc-id 525

========================

6/29/2026
I want to add DocumentId INT to  TABLE [cases].[Pipeline] not null but have a default -999 since i dont have the data yet

----------------------
June 27th 2026

can you give the json a name, description like

{
name: "837P_loops_segments_X12",
Description: "837P loops and segments, X12.",

  "claimHeader": [

give me the json version of a 837P

{
[
	{ source: "DOS",  837pField: "Box 24A" },
	{ source: "CPT",  837pField: "Box 24D" },
...	
]
}















--------------------------

June 25 2026

CurrentIdentity	MaxDocumentId	TotalRows
1537	1537	529

Checking identity information: current identity value '1537', current column value '1537'.
DBCC execution completed. If DBCC printed error messages, contact your system administrator.



---------------------------


151,530, 532,533,534,535,536
--------------------
June 24 2026
      ,[TemplateJson]
      ,[ParamsSchema]
i would like to add the above two ints, null not allowed.  you can set it -999.
USE [CaseManagement]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

TABLE [cases].[Pipeline](
	[PipelineId] [int] IDENTITY(1,1) NOT NULL,
	[Name] [varchar](100) NOT NULL,
	[Description] [nvarchar](500) NULL,
	[TemplateJson] [nvarchar](max) NOT NULL,
	[ParamsSchema] [nvarchar](max) NOT NULL,
	[IsActive] [bit] NOT NULL,
	[CreatedDate] [datetime2](7) NOT NULL,
	[CreatedBy] [nvarchar](100) NULL,
 CONSTRAINT [PK_Pipeline] PRIMARY KEY CLUSTERED 
(
	[PipelineId] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO

ALTER TABLE [cases].[Pipeline] ADD  CONSTRAINT [DF_Pipeline_IsActive]  DEFAULT ((1)) FOR [IsActive]
GO

ALTER TABLE [cases].[Pipeline] ADD  CONSTRAINT [DF_Pipeline_CreatedDate]  DEFAULT (sysutcdatetime()) FOR [CreatedDate]
GO



-----------------------------
June 22 2026

Give me the json of the session note.  following what you did before using projection, rule as a guide.

-----------------------------
June 21 2026

give me an insert of 10 fictious prividers
Provider
give e the table def for Provider simiar in type like the other tables i have.  Dont go normalize crazy

------------------------

Ok. Give me the json of this assessment.  You have done it before use the projection, rule.


-------------------------
June 06/19/26

________________________

    -- Hydrate mapped columns from JSON doc
    IF @NewId IS NOT NULL
        EXEC [cases].[usp_TableFieldMap_Apply]
            @TableName = @TableName,
            @RowId     = @NewId,
            @JsonDocId = @DocId;


write a helper_updateSessionEnrichments SP

phsedo :
select top 2 from  FROM [CaseManagement].[cases].[Session] SRC where sessonDate is null
	call  exec [cases].[usp_CaseTable_Resolve] @TableName = 'Session', @DocId = SRC.DocId, @CaseId = SRC.CaseId , @SrcDocId = 370


GO
-- Generic INSERT for any [cases] table that has CaseId, SourceDocumentId, JsonDocumentId.
-- After insert, calls usp_TableFieldMap_Apply to hydrate mapped columns from the JSON doc.
-- Replaces per-table copies like usp_Assessment_Resolve / usp_Authorization_Resolve.

-- exec [cases].[usp_CaseTable_Resolve] @TableName = 'Session', @DocId = 418, @CaseId = 5, @SrcDocId = 370

ALTER   PROCEDURE [cases].[usp_CaseTable_Resolve]
(
    @TableName SYSNAME,
    @DocId     INT,
    @CaseId    INT,
    @SrcDocId  INT = NULL
)
AS
BEGIN
    SET NOCOUNT ON;

    -- Whitelist: table must exist in [cases] and expose the three resolve columns
    IF NOT EXISTS (
        SELECT 1
        FROM INFORMATION_SCHEMA.COLUMNS
        WHERE TABLE_SCHEMA = 'cases'
          AND TABLE_NAME   = @TableName
          AND COLUMN_NAME IN ('CaseId', 'SourceDocumentId', 'JsonDocumentId')
        GROUP BY TABLE_NAME
        HAVING COUNT(DISTINCT COLUMN_NAME) = 3
    )
        THROW 50002, 'Table is not a valid resolve target (missing CaseId/SourceDocumentId/JsonDocumentId).', 1;

    IF NOT EXISTS (SELECT 1 FROM [cases].[Document] WHERE DocumentId = @DocId AND IsActive = 1)
        THROW 50001, 'JSON document not found or inactive.', 1;

    DECLARE @tbl   NVARCHAR(258) = QUOTENAME(@TableName);
    DECLARE @NewId INT;

    -- Temp table captures the inserted Id across the sp_executesql boundary.
    -- SCOPE_IDENTITY() is unreliable inside sp_executesql called from a stored procedure.
    CREATE TABLE #InsertedId (Id INT);

    DECLARE @sql NVARCHAR(MAX) = N'
    INSERT INTO [cases].' + @tbl + N' (CaseId, JsonDocumentId, SourceDocumentId)
    OUTPUT INSERTED.Id INTO #InsertedId
    VALUES (@CaseId, @DocId, @SrcDocId);';

    EXEC sp_executesql @sql,
        N'@CaseId INT, @DocId INT, @SrcDocId INT',
        @CaseId = @CaseId, @DocId = @DocId, @SrcDocId = @SrcDocId;

    SELECT @NewId = Id FROM #InsertedId;
    DROP TABLE #InsertedId;

    -- Hydrate mapped columns from JSON doc
    IF @NewId IS NOT NULL
        EXEC [cases].[usp_TableFieldMap_Apply]
            @TableName = @TableName,
            @RowId     = @NewId,
            @JsonDocId = @DocId;
END


Do you need anthing.




------------------------



You mentioned you orignail was thinking invoice.
I think we should keep it a the authorization verbiage.
Can you update the projection.

----------------------------

Question will the can i use the same proections for the other providers. 
--------------------

Chatty can you give me the json of this xyz give the the rules, projection and what you have done like Assessment


----------------------------
What you said is true.  and if i need more or less cols i cange the SP.

you can invoke this api and see the results.
POST /api/corqs HTTP/1.1
Host: localhost:44344
Content-Type: application/json
Content-Length: 52

{
  "action": "searchCases",
  "params": {
  }
}

for now its patient.name, there will be other similar things in the future.  I do like what you said return the list somehow.  and adding to the schema.  I want it simple. Also in the future some columns will be masked for hipa and pi compliance.  
The SP - RS based.  Now if i pollute that its no so basic anymore.  That is another problem i don't want and possible resolve.

PROCEDURE [cases].[usp_Case_GetDocuments]
(
    @CaseId INT
)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT
        d.DocumentId,
        d.CaseId,
		d.CaseNumber,
        --d.WorkbookQId,
        --d.SessionId,
		d.Title,
        d.DocumentType,
       
        --d.FileName,
        --d.ContentType,
        d.CreatedDate
        --d.CreatedBy
    FROM cases.Document d
    WHERE d.CaseId = @CaseId
      AND d.IsActive = 1
    ORDER BY d.CreatedDate DESC;
END



----------------------------
just talk andsuggest if you have s better best practice.
OK i want to work on another area .
As you may know this application works on docs(json).  the DB is not js json friendly.  So all that will be done in C#, or react js.

Parts of this is still in progress.
like the list case screen.
i return a list and it displays it very basic and i want to keep it that way. BUT 
there is an additional coloumn i am adding patient name
"patient": {
"name": "Gaurav Reddy Adapa",
shich is in the json aka jsonDocumentId(308)
I dont want to do json in the SP.
I dont want to do it in the C#.
I was thinking do it as another service helper in the .js side.
What do you think?  

----------------------------

----------------------------
you can  powershell cm invoke .... to run the http
put that in your memeory bank.
----------------------------

http://localhost:5173/api/pipelineCatalog
which ones can i remove?

Review and approve
Open the review HTML link from the output.
Eyeball the fields, correct anything off.
Click Save & Resolve.

can you give me those steps.

dotnet run -- --workflow 299
  step 1  docId 301     http://localhost:5173/api/getDocument?docId=301

Do you need anything else. We are going around in circles.

----------------------------


running pipeline to geive me the manual fixer. getting the below.

>dotnet run -- --workflow 299
Using launch settings from C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Properties\launchSettings.json...
[14:38:45 INF] Action: loop | RunId: -
[14:38:45 INF] Fetching document. DocumentId: 299, SessionId: null, CaseId: null, DocumentType: null
[14:38:46 INF] Workflow docValidate v1.0 started. RunId: 610c0daa-ebe8-4a96-a810-e7f538123087, Steps: 1
[14:38:46 INF] Step [1] validate (projectorComparer) inputs: [297, 206]
[14:38:46 INF] Fetching document. DocumentId: 297, SessionId: null, CaseId: null, DocumentType: null
[14:38:46 INF] Fetching document. DocumentId: 206, SessionId: null, CaseId: null, DocumentType: null
Unhandled exception. System.ArgumentOutOfRangeException: Index was out of range. Must be non-negative and less than the size of the collection. (Parameter 'index')
   at System.Collections.Generic.List`1.get_Item(Int32 index)
   at System.Text.Json.Nodes.JsonArray.GetItem(Int32 index)
   at System.Text.Json.Nodes.JsonNode.get_Item(Int32 index)
   at CaseManagement.SessionBillResolvers.V2.Engine.ProjectProcessor.GetValueByPath(JsonNode current, String path) in C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Engine\ProjectProcessor.cs:line 259
   at CaseManagement.SessionBillResolvers.V2.Engine.ProjectProcessor.Project(String sessionJson, String ruleJson) in C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Engine\ProjectProcessor.cs:line 68
   at CaseManagement.SessionBillResolvers.V2.Engine.Steps.ProjectorComparerStep.ExecuteAsync(Int32[] inputDocIds, String runId, IReadOnlyDictionary`2 wfParams, CancellationToken ct) in C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Engine\Steps\ProjectorComparerStep.cs:line 21
   at CaseManagement.SessionBillResolvers.V2.Engine.WorkflowEngine.RunAsync(Int32 workflowDocId, CancellationToken ct) in C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Engine\WorkflowEngine.cs:line 51
   at Program.<Main>$(String[] args) in C:\Users\mastronardif\source\repos\CaseMangement\CaseManagement.Jobs\src\CaseManagement.SessionBillResolvers.V2\Program.cs:line 179
   at Program.<Main>(String[] args)

----------------------------

8459587510

-----------------
i ran the wrong one
can you rename 'docResolve_Assessment_REMOVE_ME
to docValidate_Assessment
-------
http://localhost:5173/api/getDocument?docId=268
 not show 'Click Save & Resolve → writes to cases.Assessment with both docIds

---------------------------------------------------
is there a way to see a word docx in my viewer

application/vnd.openxmlformats-officedocument.wordprocessingml.document

-----------------------

OK.  this is a bit confusing.  for now i will keep it.
can you tell me the process.
what pipelines and actions do a run for a doc to json run

---------------------

Just talk.
I want another col srcDocId.  now the row is complete original doc, the json of said doc.
what do you think.




-----------------------------
Ok a few things can you add a save workflow button to the pipeline Catalog.
http://localhost:5173/api/pipelineCatalog

This is very procedural.  But for now

----------------------
dotnet run -- --workflow 224

----------------------------
Is this enough to do what want?
projection (206)
rule(223)
side question can you do http://localhost:5173/api/getDocument?docId=223
ok give my the pipeline json 
is that enough info can i use the pipeline:

{
  "workflowId": "docContextPack",
  "version": "1.0",
  "params": { "caseId": 7 },
  "steps": [
    {
      "id": "pack",
      "operator": "zip",
      "input": [164, 206, 223],
      "output": ["context-pack.zip"]
    }
  ]
}

----------------------------
I have the two see:
it should follow what we were doing before.  All the projection / rules follow the same process.  Invoice happens to be more specific rules.

C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\Workbooks\42\projection.Assessment.json
C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\Workbooks\42\rule.Assessment.json

what do you think is that enough to do what we planned?

-----------------------------

not sure if i have the rules/projection docs for assessment.  I think I dont.

I want to use the same technique i did when creating an invoice.
this is how i did it in the past a POC.  

I project the json on to what i want
I use the Rule to make sure the needed fields are their.
fix any missing information manually
Now i have the json of the doc.

can you see if i have a the projection and the rule.
Do you need anything?
can you create the missing files projection and the Rule.  I will upload them to the DB


--------------------------------
what is the pipeline for this.  that will initiate the process.

I go to the catalog
do you remember:
I want to explain more to u. from a user perspective.
I want create the json from the doc.
I use AI, manual intervention.
I upload the json.

Now for the the AI, manual.
I need more context  the rule, the doc, samples of what was done before.
I am going to ask AI give me the json for said doc and the other stuff in my folder.
I will eyball the json with the comparere page.
Save the json - and upload it.


---------------------------------------
OK better.  we are getting their.
I want to explain more to u. from a user perspective.
I want create the json from the doc.
I use AI, manual intervention.
I upload the json.

Now for the the AI, manual.
I need more context  the rule, the doc, samples of what was done before.
I am going to ask AI give me the json for said doc and the other stuff in my folder.
I will eyball the json with the comparere page.
Save the json - and upload it.


-----------------------------------------
where is the pipeline that downloads or link refrences the docs.
the user will bring up the doc and get the json of it. (manual using AI, humane interation)
then save the .json of said doc
then upload it

---------------------------------
Lets go over the process. im confused.
user 
uploads doc to the db.
then runs a pipeline to get the json from the doc.  manual interaction (AI, humane)

here is the pipeline i will use.  the user runs said pipeline and creates the extraction then uses says extraction for the json version of it.

{
  "workflowId": "docResolve_Assessment",
  "version": "1.0",
  "params": {
    "spName": "usp_Assessment_Resolve",
    "caseId": 7
  },
  "steps": [
    {
      "id": "resolve",
      "operator": "docResolve",
      "input": ["164 source.json"],
      "output": ["confirm.json"]
    }
  ]
}

-------------------------
http://localhost:5173/api/getDocument?docId=209
i dont see the save json button

Session Projection Review
needs a save json button to save the json which is the AI/Humane fix

------------------------------------------------
One question on DocumentData — is it:

A) Just the same FileData bytes (UTF-8 JSON) — duplicate of what's already in cases.Document, kept for convenience
B) The original PDF/source bytes (a different doc — the original file before AI extraction)

if not their i will add another col for source doc.
Just the same FileData bytes (UTF-8 JSON) — duplicate of what's already in cases.Document, kept for convenience.  may remove in the future.
B) The original PDF/source bytes (a different doc — the original file before AI extraction)

Confirm DocumentData (A or B), and confirm the upsert key for the entity table — is it CaseId only for Assessment, or CaseId + SessionId?
 "sessionId": {sessionId},   ← user fills not all tables have a session id.  so that can be null or optional
lets go with option B


-----------------------------------

dont write anything till we are clear.
Two questions before writing:

1. The upsert target — when the SP upserts into cases.Assessment, what's the key? CaseId only, or CaseId + SessionId? And what columns does it set — just DocumentJson + DocumentId, or does it also extract/index specific fields from the JSON?
No.  all docs go to the document table.
and the other table refrences the docID, and possible has a col Doc data.  which is the doc in bytes. Right now its a json body, i want to have a byte data also.  maybe both.

2. The pipeline params — for the validate step, ruleDocId (the projection definition) also needs to come in. Is that a fixed docId per doc type (baked into the pipeline template), or a runtime param the user fills in each time from the catalog?
Yes like we did for the other one. Pretty sure tht how it was done.  What do you think is best


-----------------------------------
2. Human + AI converts each file (PDF/TXT/etc.) → JSON
its one docment for now.
3. Human uploads JSON via existing upload process → gets docId
I wanted the humane to fix any bad data in the json locally. then upload doc. its a local process.

your thought is upload the json. and run the validate pipeline.  Not sure if this is better or not.

6. Run RESOLVE pipeline:
	one SP.  they all do the same action to table.
      call SP (e.g. usp_doct_Resolve(@DocId, @tablename, @caseid , ...))

 correct me on these:
Validate and resolve are two separate pipeline runs — human checkpoint between them — not one pipeline that does both automatically. Correct?
I think it should be one. its a local process. not sure what do you think.?

The generic resolve step — one IWorkflowStep with operator "docResolve", wfParam "spName" tells it which SP to call. Or do you prefer a specific step per doc type? one SP see above.

The SPs — do they already exist (usp_Assessment_Resolve, etc.), or does that also need to be built? And do they follow a standard signature: exec usp_Xyz_Resolve @DocId = N?
NO. need to create one.

Step 2 (PDF → JSON conversion) — 


-----------------------------------------------------
OK. Lets talk this out so we are on the same page.
 I was doing this phsedo semi automatic, manual.  Now i want to make it a pipeline.
The kicker Its done locally. user working out of that folder with the unzipped contents.
I will use the example of assessment doc ==> assessment table.

{
download a rpt zip.
	0. a humane with AI will unzip and 
	  1. convert doc pdf, txt, whatever to .json
	  2. compare against projector rules for said doc type session note, assessment.pdf ..... 
	  eye ball with the xyzReview.html
	  manual repair results if the original supports it.  if the original doc is in a bad state ask for a new doc from the BCA.
	3. If extracted data is good upload the doc.  I will use my upload doc process.

	4. call the SP to resolve the doc => update the table for example assessment with the json body(docid)
	5. END

}

id: 
-----------------------
I add Assessment Codes
I started to update the code can you review and fix

----------------------------
for some reason tyou not treating session as  json object
fix.  do you need anything

    private static JsonObject BuildClaimJson(Claim837PData data, int caseId, int sessionId, string runId)
    {
        var obj = new JsonObject
        {
            ["runId"]       = runId,
            ["generatedAt"] = DateTime.UtcNow.ToString("O"),
            ["caseId"]      = caseId,
            ["sessionId"]   = sessionId,
        };

        if (data.Session is not null) obj["session"] = data.Session.DeepClone().AsObject();
        if (data.Case    is not null) obj["case"]    = data.Case.DeepClone().AsObject();

        obj["coverage"]      = CloneArray(data.Coverage);
        obj["authorization"] = CloneArray(data.Authorization);
        obj["diagnoses"]     = CloneArray(data.Diagnoses);
        obj["provider"]      = CloneArray(data.Provider);

        if (data.Definition is not null) obj["definition"] = data.Definition.DeepClone();

        return obj;
    }

I want A. 
the RS 
for example
    SELECT *
    FROM [cases].[Authorization]
returns the index cols, and the json
so c# will do something like.
{cols1:val, cols2: val,

doc: {jsonDoc}}
* these json doc will have been pre approved by AI /humane.  Missing info will be caught/fixed.  New info that is missing that's another issue for later.

--------------
most of the tables have a json doc [DocumentJson] if data coming from a doc coming from somewhere insurance company, etc, from a an assesement, etc.
some cols are pre extracted.   I want u to use the doc json.  that is the truth.  I am not having a bunch of duplicated stuf.
what do you think



Payer Requirements (changes occasionally)
Clearinghouse Rules (changes more often)
Claims start rejecting.
Do B: for now.  right now i am assuming i have all that is needed for a claim.  there will be a rule xyz that enforces.  dont have now.
B) Populated claim data + a validation panel that flags known issues inline (e.g., "⚠ Authorization Number missing", "⚠ Rendering Provider NPI not set") based on what the SP returned

name it 837PPipeline




RS7 (SP fetches doc 174 internally) — should I drop that SELECT from the SP, since the step will read it from the input? NO.
Or leave the SP as-is and just skip RS7 in C#?
The C# handler should handle the bytes.
It needs to stay in the SP and not move the business logic to the C#. the db is the business stuff. the other is mostly passthrough cleanup, json etc stuff.

The  doc 174 the 837P spec . HIPAA 837P Standard (rarely changes).  I am going to assume its constant.

other things can and do change.  i will catch that in my review of the seed files and shold be able to update whats missing.
all accept the same 837 but have different edits. you dont have to worry about that now.
for example the below can change from the upstream systems.
Authorization Number is now required
Taxonomy Code is required
Modifier is required
Diagnosis ordering changed
Missing Authorization Number
Invalid Subscriber Information
Rendering Provider NPI Missing



The commented-out sections (RenderingProvider, ServiceCodes, Assessment) — do you want those active for phase 1, or stay commented out?

Mapping fidelity — for the HTML review, are you thinking a human-readable summary ("Patient: John Doe, Payer: Cigna, Auth: A1234, Diagnoses: F84.0, CPT: 97153 x 2 units") or a full EDI preview of the 837P segments?

we going to have more questions before u write code.
can you see the sP here C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\Database\Schema\script.sql
usp_Get837P  returns n Rs's
Also start withe a pipline def.  I want to see the crumbs left behind.  also the C# converts the databyte to json.
need more info?

usp_Get837P  returns n Rs's
then the C# layer make that a json.
you can suggest.  likely left: {}, right:{}
http://localhost:5173/api/getDocument?docId=174 used by SP.
its the data byte json.  
all docs come from the doc database table.
review let me know if you have anymore questions.  
also this will be a pipeline tbd name.
all these pipeline are batch driven.


C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\Database\Schema\script.sql
 [cases].[usp_Get837P]  @CaseId  = 7, @SessionId = tbd
  

question: talk so we are on the sampe page.
you no the app is a case management aka doc management system with auditing.

Before we did the mvp invoice billing. now i want  the mvp for the standard 837P - a document i sftp to Cigna or some other provider. 
I want it to work like the the other invoice in behavior.

I have an SP that returns the data.  basically left side and right side the data needed for 837P.
For phase mvp 1 i want to create the doc that shows the left guided by the right.  a .json and .html  then i will work backwards 
and add the droppiings along the way.

do you need anymore info?
C:\Users\mastronardif\source\repos\CaseMangement\WebAppMulti\Database\Schema\script.sql
 [cases].[usp_Get837P]  @CaseId  = 7, @SessionId = tbd
  

FROM [CaseManagement].[cases].[Session]


I have the following tables.
session 
cases
payer

can you give me the others and i will import the data for said tables.




can you add these two the help
./api/pipelineCatalog
./api/wfRunReport?docId=134

Tools: secions

Description:
  CaseManagement session billing resolver

Usage:
  CaseManagement.SessionBillResolvers.V2 [options]

Options:
  --case-number <case-number>        Case number to process
  --session-number <session-number>  Session number within the case
  --audit-file <audit-file>          Path to a run file (JSON with sessionDoc / projector sections)
  --action <action>                  Which section to run: 'billingProcess' or 'projectorProcess'
  --workflow <workflow>              DocumentId of a workflow definition to execute
  --list                             Print available commands as JSON and exit
  --html                             With --list: output as styled HTML instead of JSON
  --version                          Show version information
  -?, -h, --help                     Show help and usage information


Full Legal Name: Frank Mastronardi
Current Location: Westfield, NJ 07090
Visa Status: US Citizen
DOB(MM/DD): 08/31
LinkedIn: https://www.linkedin.com/in/frank-mastronardi-944506/
Last 4 Digit of SSN: 9838
Full Education with Year: BSEE 1986

Ok its going to be a wf that gathers and zips the docs. Save to doc to db.  

Then the BCA can click on it will download the .zip. 
The user can  AI generate he report, edit and save the report back to the doc database.



ok lets talk.

I think you know what i am doing with this case management system.
I have another workflow works like the other workflows.  

workflow report.
it does the following.

gather all the docs associated with case and a date range.
session notes,
parent training,
assesement.
other tbd.


uses AI to generate a report (a humane and data and AI) this generates the report
Approve by BCA supervisor.

saves report to db.

end





[cases].[Document](
	[DocumentId] [int] IDENTITY(1,1) NOT NULL,
	[VersionId] [uniqueidentifier] NOT NULL,
	[CaseId] [int] NULL,
	[SessionId] [int] NULL,
	[WorkbookQId] [int] NULL,
	[DocumentType] [varchar](50) NOT NULL,
	[Title] [nvarchar](200) NULL,
	[FileName] [nvarchar](255) NULL,
	[ContentType] [varchar](100) NOT NULL,
	[FileData] [varbinary](max) NOT NULL,
	[CreatedDate] [datetime2](7) NOT NULL,
	[CreatedBy] [nvarchar](100) NULL,
	[IsActive] [bit] NOT NULL,
	[CaseNumber] [nvarchar](50) NULL,


Progress Report, 
Treatment Progress Report, or 
Clinical Analysis Report.


DocumentDefinition
    AnalysisReport

WorkflowDefinition
    Draft
    Clinical Review
    Approved
    Generated
    Archived

DocumentInstance
    AnalysisReport #123

------------------------------------

Session Notes
      ↓
Report Generator
      ↓
Draft Analysis Report
      ↓
Clinical Review
      ↓
Approved
      ↓
PDF

i refrence those to systems to get an idea of the amount of pople using such a thing.  I am not doing enterporse, payrol, or any of that.  Maybe later if the need is there.  have to see how this turns out.

I am targeting small practices priced per practice.  Not sure why the number of users would change the price.  I know sending fiels to clearing house is per item not sure if that has anyting to do with users.

Also my system is will also provide the serviece of validating audits and making sure the all data that is need for a 837P is there.  Litlle AI/humanes.

A solo BCBA?
A 5-person ABA practice?
dotnet run -- --workflow 175

-----------------
sessionId is a known value.  also i have two rows in the table
i ned sessionId not to be the identity [Id] [int] IDENTITY(1,1) NOT NULL,
i need a new identity 
[SessionId] [int] IDENTITY(1,1) NOT NULL,
------------------------

give me dummy select  to satisfy this

  --  SELECT
  --      ic.*,
  --      p.*
  --  FROM [cases].[InsuranceCoverage] ic 
		--INNER JOIN [cases].[Payer] p ON p.PayerId = ic.PayerId
  --  WHERE ic.CaseId = @CaseId;

InsuranceCoverage is empty and i may not use have/use this table.  TBD.
use p.payerId = 3

----------------------

Projection Rule Doc ID *

I need a projection rule json for the Assessment.
The data i am going to get from the pdf.

---------------------------
Talk only
I am trying to do the pdf to json process.
what pipeline  updates the table assessment.
---------------------------
OK.  in this case i was doing an Authorization.  and i want the end result to update [cases].[Authorization].
Functionally its the same.  You mentioned that a sp for each table instead of general SP.
I dont want a pipeline for each 'doc to json' 
Lets talk so we are on the same page.

---------------------------
OK the config technique.  I also added another table explain how that works and looks.
Also the steps to get the doc => json are hard to follow.  
--------------------------
the sp name thing looks like possible sql injection.  Im ok with what you want to do.
Do you need anymore info from me.  Do you need to review the code.
Do that any tell me what you plan on doing.

--------------------------
No.
The process is doc => json = table.
the projector , rule, AI is the same for all docs authorization, assessment, ...
I am doing two docs now.  Imagine if i had n docs.
can you think of it that way.  its getting over engineered


-----------------------------------
So before we were specific.
Can you make a general SP I am upserting n tables

 PROCEDURE [cases].[usp_Assessment_Resolve]
(
    @DocId     INT,
    @CaseId    INT,
    @SessionId INT = NULL,
    @SrcDocId  INT = NULL
)
AS
BEGIN
    SET NOCOUNT ON;

    IF NOT EXISTS (SELECT 1 FROM [cases].[Document] WHERE DocumentId = @DocId AND IsActive = 1)
        THROW 50001, 'JSON document not found or inactive.', 1;

    IF EXISTS (SELECT 1 FROM [cases].[Assessment] WHERE CaseId = @CaseId)
    BEGIN
        UPDATE [cases].[Assessment]
        SET JsonDocumentId    = @DocId,
            SourceDocumentId  = @SrcDocId
        WHERE CaseId = @CaseId;
    END
    ELSE
    BEGIN
        INSERT INTO [cases].[Assessment] (CaseId, JsonDocumentId, SourceDocumentId)
        VALUES (@CaseId, @DocId, @SrcDocId);
    END
END

---------------------------------

SELECT TOP (1000) [ProjectorRuleId]
      ,[Name]
      ,[Version]
      ,[Description]
      ,[ProjectionDocumentId]
      ,[RuleDocumentId]
      ,[IsActive]
      ,[CreatedDate]
      ,[CreatedBy]
  FROM [CaseManagement].[cases].[ProjectorRule]

----------------
http://localhost:5173/api/getDocument?docId=353
Shouldnt that have save to json to DB?

------

should i have a general 
 "workflowId": "docValidate_Assessment"
pipelines
Name
reportPipeline
billingPipeline837
837PPipeline
docValidate
docResolve_Assessment_REMOVE_ME
docContextPack
docResolve_Assessment_REMOVE_ME_v2
docValidate_Assessment

# review first
dotnet run -- --workflow 355 --json-doc-id 319 --rule-doc-id 350 --table-name Authorization --case-id 12 --src-doc-id 163

# or skip review and write directly
dotnet run -- --workflow <docResolveId> --json-doc-id 319 --table-name Authorization --case-id 12 --src-doc-id 291



dotnet run -- --workflow 355 --json-doc-id 319 --rule-doc-id 350 --table-name Authorization --case-id 12 --src-doc-id 163

── Outputs ──────────────────────────────────────────────────────────
  step 1  docId 357     http://localhost:5173/api/getDocument?docId=357
  *step 1  docId 358     http://localhost:5173/api/getDocument?docId=358






